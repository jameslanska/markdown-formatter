{
	"compilerOptions": {
		// Base Options
		"module": "nodenext",
		"moduleResolution": "nodenext",
		"target": "es2024",
		"lib": [
			"DOM",
			"es2024",
		],

		// TypeScript 6 no longer auto-includes all @types/* packages globally, so list the ones providing ambient globals explicitly.
		"types": [
			"node",
			"mocha",
		],

		// Output
		"outDir": "out",
		"rootDir": "src",
		"sourceMap": true,

		// Strictness
		"strict": true,

		// JavaScript Support
		"allowJs": true,
	},
	"exclude": [
		"node_modules",
		".vscode-test",
		"docs",
		"out",
		"eslint.config.mjs",
	]
}